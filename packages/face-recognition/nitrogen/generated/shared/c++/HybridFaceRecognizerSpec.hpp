///
/// HybridFaceRecognizerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `RegisteredPerson` to properly resolve imports.
namespace margelo::nitro::mlkit::recognition { struct RegisteredPerson; }
// Forward declaration of `FaceSearchResult` to properly resolve imports.
namespace margelo::nitro::mlkit::recognition { struct FaceSearchResult; }
// Forward declaration of `PhotoPersonResult` to properly resolve imports.
namespace margelo::nitro::mlkit::recognition { struct PhotoPersonResult; }
// Forward declaration of `FindPeopleOptions` to properly resolve imports.
namespace margelo::nitro::mlkit::recognition { struct FindPeopleOptions; }
// Forward declaration of `FaceEmbedding` to properly resolve imports.
namespace margelo::nitro::mlkit::recognition { struct FaceEmbedding; }

#include <NitroModules/Promise.hpp>
#include <string>
#include "RegisteredPerson.hpp"
#include <vector>
#include "FaceSearchResult.hpp"
#include "PhotoPersonResult.hpp"
#include "FindPeopleOptions.hpp"
#include <optional>
#include "FaceEmbedding.hpp"

namespace margelo::nitro::mlkit::recognition {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `FaceRecognizer`
   * Inherit this class to create instances of `HybridFaceRecognizerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridFaceRecognizer: public HybridFaceRecognizerSpec {
   * public:
   *   HybridFaceRecognizer(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridFaceRecognizerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridFaceRecognizerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridFaceRecognizerSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<bool>> downloadModel(const std::string& url) = 0;
      virtual std::shared_ptr<Promise<bool>> loadModel(const std::string& fileUri) = 0;
      virtual bool isModelReady() = 0;
      virtual std::shared_ptr<Promise<bool>> registerPerson(const std::string& id, const std::string& name, const std::string& imageUri) = 0;
      virtual std::shared_ptr<Promise<bool>> addReference(const std::string& id, const std::string& imageUri) = 0;
      virtual void removePerson(const std::string& id) = 0;
      virtual void clearRegistry() = 0;
      virtual std::vector<RegisteredPerson> getRegistry() = 0;
      virtual std::shared_ptr<Promise<std::vector<FaceSearchResult>>> findPeople(const std::string& imageUri) = 0;
      virtual std::shared_ptr<Promise<std::vector<PhotoPersonResult>>> findPeopleInPhotos(const std::vector<std::string>& imageUris, const std::optional<FindPeopleOptions>& options) = 0;
      virtual std::shared_ptr<Promise<std::optional<FaceSearchResult>>> identifyFace(const std::string& faceUri) = 0;
      virtual std::shared_ptr<Promise<FaceEmbedding>> extractEmbedding(const std::string& faceUri) = 0;
      virtual double compare(const std::vector<double>& embedding1, const std::vector<double>& embedding2) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "FaceRecognizer";
  };

} // namespace margelo::nitro::mlkit::recognition
