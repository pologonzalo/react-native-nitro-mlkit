///
/// HybridTextRecognizerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `RecognizedText` to properly resolve imports.
namespace margelo::nitro::mlkit::text { struct RecognizedText; }
// Forward declaration of `BatchTextResult` to properly resolve imports.
namespace margelo::nitro::mlkit::text { struct BatchTextResult; }

#include "RecognizedText.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include "BatchTextResult.hpp"
#include <vector>

namespace margelo::nitro::mlkit::text {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `TextRecognizer`
   * Inherit this class to create instances of `HybridTextRecognizerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridTextRecognizer: public HybridTextRecognizerSpec {
   * public:
   *   HybridTextRecognizer(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridTextRecognizerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridTextRecognizerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridTextRecognizerSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<RecognizedText>> recognize(const std::string& imageUri) = 0;
      virtual std::shared_ptr<Promise<std::string>> recognizeText(const std::string& imageUri) = 0;
      virtual std::shared_ptr<Promise<std::vector<BatchTextResult>>> recognizeBatch(const std::vector<std::string>& imageUris, double concurrency) = 0;
      virtual bool isAvailable() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "TextRecognizer";
  };

} // namespace margelo::nitro::mlkit::text
