///
/// HybridImageLabelerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `ImageLabel` to properly resolve imports.
namespace margelo::nitro::mlkit::labeling { struct ImageLabel; }
// Forward declaration of `LabelingOptions` to properly resolve imports.
namespace margelo::nitro::mlkit::labeling { struct LabelingOptions; }
// Forward declaration of `BatchLabelResult` to properly resolve imports.
namespace margelo::nitro::mlkit::labeling { struct BatchLabelResult; }
// Forward declaration of `BatchLabelOptions` to properly resolve imports.
namespace margelo::nitro::mlkit::labeling { struct BatchLabelOptions; }
// Forward declaration of `SafetyResult` to properly resolve imports.
namespace margelo::nitro::mlkit::labeling { struct SafetyResult; }
// Forward declaration of `BatchSafetyOptions` to properly resolve imports.
namespace margelo::nitro::mlkit::labeling { struct BatchSafetyOptions; }

#include "ImageLabel.hpp"
#include <vector>
#include <NitroModules/Promise.hpp>
#include <string>
#include "LabelingOptions.hpp"
#include <optional>
#include "BatchLabelResult.hpp"
#include "BatchLabelOptions.hpp"
#include "SafetyResult.hpp"
#include "BatchSafetyOptions.hpp"

namespace margelo::nitro::mlkit::labeling {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `ImageLabeler`
   * Inherit this class to create instances of `HybridImageLabelerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridImageLabeler: public HybridImageLabelerSpec {
   * public:
   *   HybridImageLabeler(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridImageLabelerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridImageLabelerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridImageLabelerSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::vector<ImageLabel>>> label(const std::string& imageUri, const std::optional<LabelingOptions>& options) = 0;
      virtual std::shared_ptr<Promise<std::vector<BatchLabelResult>>> labelBatch(const std::vector<std::string>& imageUris, const std::optional<BatchLabelOptions>& options) = 0;
      virtual std::shared_ptr<Promise<SafetyResult>> checkSafety(const std::string& imageUri) = 0;
      virtual std::shared_ptr<Promise<std::vector<SafetyResult>>> checkSafetyBatch(const std::vector<std::string>& imageUris, const std::optional<BatchSafetyOptions>& options) = 0;
      virtual std::shared_ptr<Promise<std::vector<ImageLabel>>> matchCategories(const std::string& imageUri, const std::vector<std::string>& categories) = 0;
      virtual bool isAvailable() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "ImageLabeler";
  };

} // namespace margelo::nitro::mlkit::labeling
