///
/// HybridBarcodeScannerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `Barcode` to properly resolve imports.
namespace margelo::nitro::mlkit::barcode { struct Barcode; }
// Forward declaration of `BatchScanResult` to properly resolve imports.
namespace margelo::nitro::mlkit::barcode { struct BatchScanResult; }

#include "Barcode.hpp"
#include <vector>
#include <NitroModules/Promise.hpp>
#include <string>
#include "BatchScanResult.hpp"
#include <optional>

namespace margelo::nitro::mlkit::barcode {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `BarcodeScanner`
   * Inherit this class to create instances of `HybridBarcodeScannerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridBarcodeScanner: public HybridBarcodeScannerSpec {
   * public:
   *   HybridBarcodeScanner(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridBarcodeScannerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridBarcodeScannerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridBarcodeScannerSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::vector<Barcode>>> scan(const std::string& imageUri) = 0;
      virtual std::shared_ptr<Promise<std::vector<BatchScanResult>>> scanBatch(const std::vector<std::string>& imageUris, double concurrency) = 0;
      virtual std::shared_ptr<Promise<std::optional<Barcode>>> scanFirst(const std::string& imageUri) = 0;
      virtual bool isAvailable() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "BarcodeScanner";
  };

} // namespace margelo::nitro::mlkit::barcode
