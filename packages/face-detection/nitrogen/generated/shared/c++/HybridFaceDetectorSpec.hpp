///
/// HybridFaceDetectorSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `DetectedFace` to properly resolve imports.
namespace margelo::nitro::mlkit::face { struct DetectedFace; }
// Forward declaration of `FaceDetectionOptions` to properly resolve imports.
namespace margelo::nitro::mlkit::face { struct FaceDetectionOptions; }
// Forward declaration of `BatchCropResult` to properly resolve imports.
namespace margelo::nitro::mlkit::face { struct BatchCropResult; }
// Forward declaration of `CroppedFace` to properly resolve imports.
namespace margelo::nitro::mlkit::face { struct CroppedFace; }
// Forward declaration of `BatchEmbeddingResult` to properly resolve imports.
namespace margelo::nitro::mlkit::face { struct BatchEmbeddingResult; }

#include "DetectedFace.hpp"
#include <vector>
#include <NitroModules/Promise.hpp>
#include <string>
#include "FaceDetectionOptions.hpp"
#include "BatchCropResult.hpp"
#include <optional>
#include "CroppedFace.hpp"
#include "BatchEmbeddingResult.hpp"

namespace margelo::nitro::mlkit::face {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `FaceDetector`
   * Inherit this class to create instances of `HybridFaceDetectorSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridFaceDetector: public HybridFaceDetectorSpec {
   * public:
   *   HybridFaceDetector(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridFaceDetectorSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridFaceDetectorSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridFaceDetectorSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<std::vector<DetectedFace>>> detect(const std::string& imageUri, const FaceDetectionOptions& options) = 0;
      virtual std::shared_ptr<Promise<std::vector<BatchCropResult>>> detectBatch(const std::vector<std::string>& imageUris, double concurrency, const std::optional<FaceDetectionOptions>& options) = 0;
      virtual std::shared_ptr<Promise<DetectedFace>> detectPrimary(const std::string& imageUri) = 0;
      virtual std::shared_ptr<Promise<std::vector<CroppedFace>>> cropFaces(const std::string& imageUri, double padding) = 0;
      virtual std::shared_ptr<Promise<std::vector<double>>> extractEmbedding(const std::string& faceUri) = 0;
      virtual double compareFaces(const std::vector<double>& embedding1, const std::vector<double>& embedding2) = 0;
      virtual std::shared_ptr<Promise<std::vector<double>>> extractPrimaryEmbedding(const std::string& imageUri) = 0;
      virtual std::shared_ptr<Promise<std::vector<BatchEmbeddingResult>>> detectAndEmbed(const std::vector<std::string>& imageUris, double concurrency) = 0;
      virtual bool isAvailable() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "FaceDetector";
  };

} // namespace margelo::nitro::mlkit::face
